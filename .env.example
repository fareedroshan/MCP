# Example .env file for AI Agent
# Copy this to .env and fill in your actual credentials and keys
# IMPORTANT: Add .env to your .gitignore file to avoid committing secrets!

# OpenAI API Key (Required for LLM queries)
OPENAI_API_KEY="sk-your_openai_api_key_here"

# AI Agent Encryption Key (Optional but recommended for credential persistence)
# If not set, SecurityManager will generate one (ephemeral if not saved by user)
# Generate a strong key, e.g., using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AI_AGENT_ENCRYPTION_KEY="your_secure_encryption_key_for_credentials_here"

# --- Device Specific Credentials (Optional - can be set interactively) ---
# If you prefer to set credentials via environment variables instead of the encrypted store,
# the SecurityManager has a basic fallback. Use the pattern: <DEVICE_ID>_USERNAME and <DEVICE_ID>_PASSWORD
# Replace <DEVICE_ID> with the actual ID you use for the device in the agent.
# Example:
# MY_LINUX_SERVER_USERNAME="myuser"
# MY_LINUX_SERVER_PASSWORD="mypassword"

# CISCO_ROUTER_USERNAME="cisco"
# CISCO_ROUTER_PASSWORD="supersecretpassword"
